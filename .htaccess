<IfModule mod_rewrite.c>
    RewriteEngine On

    # Redirect all requests into the public directory.
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>

# Disable directory browsing.
Options -Indexes
