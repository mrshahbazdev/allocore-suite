<div>
    @include('invoicemaker::partials.nav')
    <div class="mb-6"><h1 class="text-2xl font-bold text-slate-900">{{ __('Cash book') }}</h1><p class="text-sm text-slate-500">{{ __('Automatic postings from invoice payments and expenses.') }}</p></div>
    <div class="mb-6 grid gap-4 sm:grid-cols-3"><div class="rounded-xl border border-slate-200 bg-white p-5"><p class="text-sm text-slate-500">{{ __('Income') }}</p><p class="mt-2 text-2xl font-bold text-emerald-700">{{ number_format((float) $income, 2) }}</p></div><div class="rounded-xl border border-slate-200 bg-white p-5"><p class="text-sm text-slate-500">{{ __('Expenses') }}</p><p class="mt-2 text-2xl font-bold text-rose-700">{{ number_format((float) $expenses, 2) }}</p></div><div class="rounded-xl border border-slate-200 bg-white p-5"><p class="text-sm text-slate-500">{{ __('Balance') }}</p><p class="mt-2 text-2xl font-bold text-slate-900">{{ number_format((float) $income - (float) $expenses, 2) }}</p></div></div>
    <div class="rounded-xl border border-slate-200 bg-white shadow-sm"><div class="border-b border-slate-200 p-4"><select wire:model.live="type" class="rounded-lg border-slate-300 text-sm"><option value="">{{ __('All entries') }}</option><option value="income">{{ __('Income') }}</option><option value="expense">{{ __('Expense') }}</option></select></div><div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-200 text-sm"><thead class="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th class="px-5 py-3">{{ __('Booking') }}</th><th class="px-5 py-3">{{ __('Date') }}</th><th class="px-5 py-3">{{ __('Partner') }}</th><th class="px-5 py-3">{{ __('Description') }}</th><th class="px-5 py-3 text-right">{{ __('Amount') }}</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($entries as $entry)<tr><td class="px-5 py-4 font-medium">{{ $entry->booking_number }}</td><td class="px-5 py-4 text-slate-600">{{ $entry->date->format('M d, Y') }}</td><td class="px-5 py-4 text-slate-600">{{ $entry->partner_name ?: '—' }}</td><td class="px-5 py-4 text-slate-600">{{ $entry->description }}</td><td class="px-5 py-4 text-right font-medium {{ $entry->type === 'income' ? 'text-emerald-700' : 'text-rose-700' }}">{{ $entry->type === 'income' ? '+' : '-' }}{{ number_format((float) $entry->amount, 2) }}</td></tr>@empty<tr><td colspan="5" class="px-5 py-10 text-center text-slate-500">{{ __('No cash-book entries.') }}</td></tr>@endforelse</tbody></table></div><div class="border-t border-slate-200 p-4">{{ $entries->links() }}</div></div>
</div>
